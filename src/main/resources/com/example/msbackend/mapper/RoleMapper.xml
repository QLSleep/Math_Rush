<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.msbackend.mapper.RoleMapper">
    
    <!-- 根据角色名获取角色ID -->
    <select id="getRoleIdByRoleName" resultType="java.lang.Integer">
        SELECT
            id
        FROM sys_role
        WHERE name = #{roleName}
    </select>
</mapper>